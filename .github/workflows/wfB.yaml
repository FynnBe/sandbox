name: wfB

on:
  workflow_run:
    workflows: [wfA]
    types: [completed]

jobs:
  some-job:
    runs-on: ubuntu-latest
    steps:
      - name: github.event.number ${{ github.event.number }}
        run: echo here
      - name: context.workflow.event.number ${{ context.workflow.event.number }}
        run: echo here
      - name: context.workflow.github.event.number ${{ context.workflow.github.event.number }}
        run: echo here
      - name: context.workflow.event_name ${{ context.workflow.event_name }}
        run: echo here
